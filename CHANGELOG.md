# 飞牛影视 v2.9.1

发布日期：2026-09-15

## 更新内容

- 隐私模式入口优化：由按钮改为「飞牛媒体库」右侧的**小三角下拉**（更紧凑，紧贴标题），点击选择「媒体库 / 隐私模式」

# 飞牛影视 v2.9.0

发布日期：2026-09-15

## 更新内容（隐私模式）

- 新增**隐私模式**：顶栏「飞牛媒体库」右侧下拉框可切换进入，进入需输入密码
  - 首次进入引导设置密码（至少 4 位，scrypt 加盐哈希存储，不可逆）
  - **每次退出或关闭应用重启后都需要重新输入密码**（解锁状态仅存内存）
  - 忘记密码可在解锁弹窗「重置密码」：清除后重启应用重设，已扫描内容与观看进度全部保留
- **双向完全隔离**：正常界面看不到隐私模式的任何内容（媒体、搜索、继续观看、源管理、刮削全部隔离），隐私模式内也看不到正常界面的任何内容（独立继续观看、无类别筛选）
- **隐私源严格按文件夹聚合**：一个文件夹 = 一部"剧集"（标题=文件夹名，集号按文件名自然排序），源根散文件为独立条目
- 隐私源**仅扫描不刮削**：不联网、不读 NFO、无手动匹配；封面自动使用文件夹内 `folder.jpg`/`poster.jpg` 等本地图片
- 观看进度、续播、已看标记在隐私模式下正常保存
- 说明：隐私模式为"界面隐藏级"隐私（内容未加密），用于避免媒体在正常界面暴露

## 升级说明

- 覆盖安装即可，媒体库数据与观看记录保留
- 隐私源请在隐私模式内的「源管理」中添加（正常模式的源管理中不会出现任何隐私痕迹）

# 飞牛影视 v2.8.2

发布日期：2026-09-15

## 更新内容（对齐飞牛：源"内容类型"）

- 媒体源新增「内容类型」（对齐飞牛影视的建库"内容类型"）：**电影 / 剧集 / 动漫 / 其他视频**
  - 添加源时可手动选择，留空则按名称/路径自动识别（如"动漫"、"电影"、"国产剧"）
  - 源管理列表中可直接修改（飞牛为创建后不可改，这里可随时调整）
  - 旧源自动按名称补齐类型，无需重新扫描
- 顶部筛选改为内容类型：**全部 / 电影 / 剧集 / 动漫 / 其他视频**（动漫源下的剧集与电影会一起展示）
- 该分类与飞牛一致：仅用于管理/筛选，不影响解析与刮削

## 升级说明

- 覆盖安装即可，媒体库数据与观看记录保留，无需重新扫描

# 飞牛影视 v2.8.1

发布日期：2026-09-15

## 更新内容（解析通用化 + 体验修复）

- 剧集解析通用化（不再针对个别目录名打补丁）：
  - 支持 `Season 1 异人篇` 这类带副标题的季目录，同剧各季正确合并
  - 季目录之下的任何目录（版本/画质/合集，如 `【4K.60帧收藏版】`）一律透明，不再被误判为独立剧
  - 方括号发布命名（如 `[DBD-Raws][Kaijuu 8 Gou][01][1080P]`、`[YYDM-11FANS][Fullmetal Alchemist][51]`）不再因"清洗后标题为空"被整包丢弃
  - 绝对集号取最右候选（`ANi_怪獣_8_號_第二季_13_...` 不再全部误判为第 8 集）
  - 支持 Jellyfin 规范的多集文件 `S01E01-E02`（记为第一集，区间标记不混入集标题）
  - 剧集分组键只使用目录派生的年份，文件名中的年份不再把同剧按季拆散
- 刮削失败提示：全部失败时显示红色提示并附失败原因（网络/TMDB 错误），不再静默
- 观看状态文案：卡片与详情显示「未看 / 已看 x/y / 已看完」，不再一律显示"已看"
- 新增命名语料回归测试（真实压制组/中文剧集/Jellyfin 规范样例），共 153 项测试全绿

## 升级说明

- 覆盖安装即可，媒体库数据与观看记录保留
- 建议升级后对源执行一次「重新扫描」以应用新的解析规则
- 在线刮削需在「设置」中填写 TMDB API Key（无网络/代理不通时会在界面提示失败原因）

# 飞牛影视 v2.8.0

发布日期：2026-09-14

## 更新内容（本地媒体库增强）

- 剧集识别大幅增强：支持 `S01 2016` / `Season 2` / `第X季`（含中文数字）/ `Specials` 等季目录形态，支持 `S01E01`、`EP01`、`[01]`、`01 - 标题`、`01_标题` 等多种集号命名
- 同剧多季正确聚合：同一部剧的多个季目录合并为一张卡片，多季显示季标签，季级详情与季专属海报（TMDB）
- 特别篇 / 番外 / 前瞻篇自动归入第 0 季；花絮、OP/ED/NCED/PV/映像特典自动过滤，不再入库
- 目录名内嵌 TMDB ID（如 `｛tmdb-259537｝`、`[tmdbid-88319]`）时直接按 ID 刮削，匹配 100% 准确，并自动从剧名中剥离该标记
- 发行噪音防护：`1080p` / `x264` / `BDRip` 等命名中的数字不再被误判为集号，电影不再被误识别为剧集
- WebDAV 源支持：远程目录扫描、凭据安全存储（safeStorage）、经本地代理的鉴权播放
- 跳过片头片尾本地化：跳过信息存于本地，代理会话接口读写，播放器联动
- 播放体验完善：多集连播跨季连续、继续观看竖版缩略图、观看筛选按季语义（未看＝该季有未看集）
- 卡片文案修正：观看进度显示为「已看 X/Y」，避免误读为状态标记

## 升级说明

- 覆盖安装即可，媒体库数据（SQLite）与观看记录自动保留
- 建议升级后对源执行一次「重新扫描」，以应用新的剧集识别规则
- 在线刮削需在「设置」中填写 TMDB API Key

# 飞牛影视 v2.7.0

发布日期：2026-09-06

## 更新内容（本地媒体管理器）

本版本将应用从 fnOS 专用客户端升级为多源本地媒体管理器：

- 新增统一媒体库：本地磁盘 / SMB 目录扫描入库，自动识别电影与剧集（支持中英文命名、季集号、多版本）
- 新增 TMDB 在线刮削：自动拉取海报、简介、评分、集名；API 与图片域名可配置镜像以适配国内网络
- 新增本地播放链：点击库内条目直接唤起 MPV 播放，支持多集连播、断点续播（进度存本地库）
- 新增 STRM 直链文件支持：`.strm` 文件内的 URL 可直接播放
- 新增本地字幕支持：自动挂载视频同目录的 srt/ass/ssa/sup/vtt 字幕
- 新增观看状态管理：已看/未看标记、自动观看标记、继续观看、观看历史（全部本地存储）
- 新增媒体库页面：海报墙 / 列表双视图、搜索、筛选排序、源管理、刮削设置，应用启动直接进入媒体库
- 媒体库数据存储于 SQLite（`~/.fntv/library/`），不可用时自动降级为 JSON 存储
- NFO 与本地海报兼容：自动读取 Kodi/Jellyfin 风格 `.nfo` 与 poster.jpg / folder.jpg
- 本地代理（Proxy）服务改为可选组件：缺失或启动失败时仅提示告警，本地播放不受影响

## 保留的原有能力

- MPV 播放增强完整保留：弹幕自动匹配（uosc_danmaku）、Anime4K 着色器、跳过片头片尾（章节/手动/快捷键）、音量记忆
- fnOS 登录体系与 fn_api、本地代理播放链保留在代码中，将作为后续 fnOS 源适配回归
- 窗口管理、系统托盘、更新检查、日志脱敏、证书信任等系统能力不变

## 升级说明

- 首次使用：在「源管理」中添加本地文件夹并扫描，即可在海报墙浏览与播放
- 在线刮削需在「设置」中填写 TMDB API Key（可在 https://www.themoviedb.org 免费申请）；国内网络可将 API / 图片地址替换为镜像
- WebDAV 源与 fnOS NAS 源适配将在后续版本回归

## 支持平台

- Windows x64
- macOS Intel / Apple Silicon
- Linux x64 / ARM64
