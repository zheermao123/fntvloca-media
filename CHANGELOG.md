# 飞牛影视 v2.7.0

发布日期：2026-09-06

## 更新内容（本地媒体管理器）

本版本将应用从 fnOS 专用客户端升级为多源本地媒体管理器：

- 新增统一媒体库：本地磁盘 / SMB 目录扫描入库，自动识别电影与剧集（支持中英文命名、季集号、多版本）
- 新增 TMDB 在线刮削：自动拉取海报、简介、评分、集名；API 与图片域名可配置镜像以适配国内网络
- 新增本地播放链：点击库内条目直接唤起 MPV 播放，支持多集连播、断点续播（进度存本地库）
- 新增 STRM 直链文件支持：`.strm` 文件内的 URL 可直接播放
- 新增本地字幕支持：自动挂载视频同目录的 srt/ass/ssa/sup/vtt 字幕
- 新增观看状态管理：已看/未看标记、自动观看标记、继续观看、观看历史（全部本地存储）
- 新增媒体库页面：海报墙 / 列表双视图、搜索、筛选排序、源管理、刮削设置，应用启动直接进入媒体库
- 媒体库数据存储于 SQLite（`~/.fntv/library/`），不可用时自动降级为 JSON 存储
- NFO 与本地海报兼容：自动读取 Kodi/Jellyfin 风格 `.nfo` 与 poster.jpg / folder.jpg
- 本地代理（Proxy）服务改为可选组件：缺失或启动失败时仅提示告警，本地播放不受影响

## 保留的原有能力

- MPV 播放增强完整保留：弹幕自动匹配（uosc_danmaku）、Anime4K 着色器、跳过片头片尾（章节/手动/快捷键）、音量记忆
- fnOS 登录体系与 fn_api、本地代理播放链保留在代码中，将作为后续 fnOS 源适配回归
- 窗口管理、系统托盘、更新检查、日志脱敏、证书信任等系统能力不变

## 升级说明

- 首次使用：在「源管理」中添加本地文件夹并扫描，即可在海报墙浏览与播放
- 在线刮削需在「设置」中填写 TMDB API Key（可在 https://www.themoviedb.org 免费申请）；国内网络可将 API / 图片地址替换为镜像
- WebDAV 源与 fnOS NAS 源适配将在后续版本回归

## 支持平台

- Windows x64
- macOS Intel / Apple Silicon
- Linux x64 / ARM64
